<Window x:Class="Ide.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ide"
        mc:Ignorable="d"
        Title="Settings"
        Height="600" Width="800" ResizeMode="NoResize"
        Icon="Resources/Settings_16x.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TabControl BorderThickness="0,1,0,0">
            <TabItem Header="General">
                <!--TODO-->
            </TabItem>

            <TabItem Header="Languages">
                <!--TODO-->
            </TabItem>

            <TabItem Header="Frameworks">
                <!--TODO-->
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal" Margin="5">
            <!--TODO: Make prettier-->
            <Button Content="OK"  IsDefault="True" Width="100" Margin="0,0,10,0" Padding="5,0,5,0" Click="ConfirmSettings"/>
            <Button Content="Cancel" IsCancel="True" Width="100" Padding="5,0,5,03"/>
        </StackPanel>
    </Grid>
</Window>
