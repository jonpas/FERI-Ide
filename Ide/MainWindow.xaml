<Window x:Class="Ide.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ide"
        mc:Ignorable="d"
        Title="Ide IDE" Height="700" Width="1200" MinWidth="600" MinHeight="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <!--Menu-->
        <StackPanel Grid.ColumnSpan="3">
            <Menu>
                <MenuItem Header="File">
                    <MenuItem Header="Create Project..." Click="CreateProject"/>
                    <MenuItem Header="Close Project" Click="CloseProject"/>
                    <Separator/>
                    <MenuItem Header="Exit" Click="Exit"/>
                </MenuItem>

                <MenuItem Header="Edit">
                    <MenuItem Header="Undo"/>
                    <MenuItem Header="Redo"/>
                </MenuItem>

                <MenuItem Header="View">
                    <MenuItem Header="Word Wrap" IsCheckable="True"/>
                    <!--@todo implement functionality-->
                </MenuItem>

                <MenuItem Header="Tools">
                    <MenuItem Header="Build"/>
                    <Separator/>
                    <MenuItem Header="Settings" Click="OpenSettings"/>
                </MenuItem>
            </Menu>

            <!--Tool Bar-->
            <ToolBar>
                <!--@todo Use add Visual Studio Image Library icons-->
                <Button Content="Create File" Click="CreateFile"/>
                <Button Name="DeleteFileButton" Content="Delete File" IsEnabled="False" Click="DeleteFile"/>
                <Separator/>
                <Button Content="Undo"/>
                <Button Content="Redo"/>
            </ToolBar>
        </StackPanel>


        <!--Folder Structure View-->
        <TreeView Name="ProjectTree" Grid.Row="1" BorderThickness="0" SelectedItemChanged="ListClassMethods"/>

        <!--Resize Splitters-->
        <GridSplitter Grid.Row="1" Grid.Column="1" Grid.RowSpan="3" Width="2" HorizontalAlignment="Stretch" Background="LightGray"/>
        <GridSplitter Grid.Row="2" Height="2" HorizontalAlignment="Stretch" Background="LightGray"/>

        <!--Hierarchy View-->
        <ListView Name="MethodList" Grid.Row="3" BorderThickness="0"/>

        <!--Text Editor-->
        <TextBox Grid.Column="2" Grid.Row="1" Grid.RowSpan="3" BorderThickness="0" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Auto" TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True"/>

        <!--Status Bar-->
        <StatusBar Grid.Row="4" Grid.ColumnSpan="3">
            <StatusBarItem Content="Status:"/>
        </StatusBar>
    </Grid>
</Window>
